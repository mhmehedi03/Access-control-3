# Access-control-3
User ID controlled by request parameter
find API key of amdin by id parameter
1 login
2 capture the req by burp
3 then change id parameter and find the API key
4 reload the web page, then submit the api key 
